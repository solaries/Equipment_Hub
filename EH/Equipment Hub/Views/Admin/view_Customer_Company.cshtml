@model IEnumerable<Equipment_hub.Data.Models.Equipment_hub_Customer_Company>
 
 
<div class="card">
    <div class="card-body">
        <h4 class="card-title">View Customer Company</h4> 
        <div class="table-responsive">
            <table class="table table-striped table-bordered zero-configuration">
                <thead>
                    <tr>
                        <th>   </th> 
                        <th>Company Name</th>
 
                    </tr> 
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>                            
                            <td>@Html.ActionLink("Select", "edit_Customer_Company", new
                           {
                            id=item.Id, 
                            Company_Name = item.Company_name
                               })</td> 
                           <td>@Html.DisplayFor(modelItem => item.Company_name)</td>
                        </tr>
                    }
                    
                </tbody>
                <tfoot>
                    <tr>                        
                        <th>   </th> 
                        <th>Company Name</th>
 
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div> 
<script> 
    function msg(txt) {
        sweetAlert("...", txt, "info");
    }
    var statusMessage = "@ViewBag.status" || false; 
    if (statusMessage != false && statusMessage.length > 0) {
        msg(statusMessage); 
    }
</script>