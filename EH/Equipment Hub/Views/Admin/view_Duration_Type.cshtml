@model IEnumerable<Equipment_hub.Data.Models.Equipment_hub_Duration_Type>
 
 
<div class="card">
    <div class="card-body">
        <h4 class="card-title">View Duration Type</h4> 
        <div class="table-responsive">
            <table class="table table-striped table-bordered zero-configuration">
                <thead>
                    <tr>
                        <th>   </th> 
                        <th>Duration Name</th>
 
                    </tr> 
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>                            
                            <td>@Html.ActionLink("Select", "edit_Duration_Type", new
                           {
                            id=item.Id, 
                            Duration_Name = item.Duration_name
                               })</td> 
                           <td>@Html.DisplayFor(modelItem => item.Duration_name)</td>
                        </tr>
                    }
                    
                </tbody>
                <tfoot>
                    <tr>                        
                        <th>   </th> 
                        <th>Duration Name</th>
 
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div> 
<script> 
    function msg(txt) {
        sweetAlert("...", txt, "info");
    }
    var statusMessage = "@ViewBag.status" || false; 
    if (statusMessage != false && statusMessage.length > 0) {
        msg(statusMessage); 
    }
</script>